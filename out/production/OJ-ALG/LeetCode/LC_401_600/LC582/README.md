# 582: Kill Process

### Solution 1: DFS
**Time Complexity:** `O(N)`, `N` is the size of `ppid`.

**Space Complexity:** `O(N)`.

### Solution 2: BFS
**Time Complexity:** `O(N)`, `N` is the size of `ppid`.

**Space Complexity:** `O(N)`.