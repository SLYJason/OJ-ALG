# Find Root of N-Ary Tree

### Solution 1: HashSet
**Time Complexity:** `O(N)`.

**Space Complexity:** `O(N)`.

### Solution 2: Bit Manipulation
Use `XOR` features:

* `A XOR A = 0`.
* `A XOR 0 = A`.

**Time Complexity:** `O(N)`.

**Space Complexity:** `O(1)`.